# HANDOVER

> 会话代际交接文档。每代会话接手时先读本文件，完成任务后更新「当前状态」与「欠账清单」。
> 本文件记"工作现场"；已发布的版本变更记 [CHANGELOG.md](CHANGELOG.md)；开发规范与不变量见 [README.md](README.md)「开发与接手」节。

最后更新：2026-09-03（四代会话）

## 当前状态

- 版本：**v1.4.2**（版本号已同步四处，工作区改动待用户提交；提交后需打 tag v1.4.2）
- 测试：85 passed（Python 3.14.2 本机实测；CI 覆盖 3.10/3.12/3.13 三平台）
- 文档：README.md + CHANGELOG.md + 本文件。项目规模小，不建 AGENTS.md，开发者接手信息在 README「开发与接手」节
- dict-build skill（`~/.agents/skills/dict-build/`）已被用户手动删除（2026-09-03），此后不再维护，无需同步

## 四代会话工作记录（2026-09-03）

1. **项目自审计**：代码与 README 一致性 ✅（13 个 CLI 参数、阈值、输出格式逐项核对）；checkpoint 恢复矩阵、分桶 fd 预算、单行模式边界拼接审查无 P0/P1 bug
2. **文档体系**：README 修正（五步→四步、--work-dir 互斥说明、新增「开发与接手」节含 6 条设计不变量）、版本历史迁至 CHANGELOG.md、新建本文件
3. **文本合规审查**：隐私/违法/破解黑客/特定公司名称 四项全过，零命中
4. **BIG5 检测修复（P1，审计中发现）**：补 BIG5 测试时暴露误判 bug，以 FFFD 比率约束修复，7 种编码场景实测全对；CHANGELOG 已记 [Unreleased]
5. **测试**：81→85（BIG5、受损 UTF-8、并行熵路径、输出降序断言）
6. **git tag v1.4.1 补打**（此前 tag 只到 v1.4.0）

## 欠账清单（按优先级）

| # | 事项 | 级别 | 说明 |
|---|------|------|------|
| 1 | 提交当前工作区改动并打 tag v1.4.2 | P1 | 文档 + BIG5 修复 + 新测试；版本号四处已同步为 1.4.2，CHANGELOG 已含 [1.4.2] 条目 |
| 2 | pyproject classifiers 补 3.14 | P3 | 本机 3.14 实测通过，下个版本发布时顺手加上（CI 矩阵可评估加 3.14） |

## 低风险观察项（暂不需处理）

- `_detect_file_encoding` 中 `except (UnicodeDecodeError, LookupError)` 为不会触发的死分支（`errors="replace"` 不抛异常），无害
- 背压弹出 `futures.pop(0).get()` 若 worker 异常会传播中止（行为正确），但进度条未优雅关闭，仅影响显示
- git 历史提交信息多为"更新"，无法追溯变更意图；建议今后提交写具体信息（旧历史不改写）

## 下一步候选任务

- 提交并发布 1.4.2（BIG5 修复值得一个补丁版本）
- 若有大规模语料实测需求，可验证分桶路径在真实 >1GB 场景的性能表现
